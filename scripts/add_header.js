// script for dynamically adding the header / nav bar to a page

function toggleCurrentNavLink() {
  // add event listeners to each of the nav-links
  // onclick, add the "current" class to the item (if not present), or remove if present
}
